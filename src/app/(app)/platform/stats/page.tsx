/**
 * Route retirée du front tenant — voir l'application `edusen_plateforme`.
 * Code d'origine (données mockées) conservé dans
 * `_archive/platform-2026-08/stats/page.tsx`.
 */
import { PlatformMoved } from '@/components/layout/platform-moved';

export default function StatsPage() {
  return <PlatformMoved />;
}
